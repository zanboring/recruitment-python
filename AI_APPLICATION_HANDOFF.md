# AI 应用开发版 · 交接文档

> 最后更新：2026-09-10　|　当前状态：**355 项测试全绿、零警告**，启动实测通过
>
> 本文档面向「接手这个仓库的人或 AI」。目标是让你**不读历史对话也能上手**：
> 知道它现在是什么样、为什么是这样、怎么跑、改坏了怎么退、还剩什么没做。

---

## 1. 项目定位

本仓库**不是** Java 版的逐接口翻译，而是用于投递 **AI 应用开发 / LLM 应用工程师** 岗位的独立后端作品。

核心叙事：以真实招聘数据为数据源，做出一个**可评估、可观测、可控成本**的 AI 招聘助手 ——
而不只是「能跑通 RAG 和工具调用」。

技术栈：FastAPI + SQLAlchemy 2.x(async) + MySQL/SQLite + DeepSeek(云端) + Ollama(本地双模型)

**规模**：9 个路由模块 / 82 个 REST 接口 / 13 个业务服务 / 7 张数据表 / 355 项测试 / 约 1.33 万行 Python

---

## 2. 五分钟上手

```bash
cd recruitment-python

# 1) 依赖（需要 Python 3.11+；若 .venv 指向 WindowsApps 路径，删掉重建）
.venv/Scripts/python.exe -m pytest tests/ -q      # 预期：355 passed

# 2) 配置：复制模板后**只需填一个 Key**
cp .env.example .env
#    编辑 .env：DEEPSEEK_API_KEY=<你的 key>     ← 从 platform.deepseek.com/api_keys 获取
#    其余项都有可用默认值；Key 留空时会自动降级到本地 Ollama / 规则引擎，功能仍可用

# 3) 建库 + 启动
.venv/Scripts/python.exe scripts/init_db.py
.venv/Scripts/python.exe -m app.main
#    打开 http://127.0.0.1:8080/docs 看接口文档
#    默认管理员：admin / admin123（**生产环境请先改掉**）
```

**不需要**先装 Ollama 才能跑：本地模型只是降级链的一环，没有它也能演示全部 AI 功能。
若要完整体验降级链，见 `app/services/ollama_client.py` 顶部的模型分工说明。

---

## 3. 备份与回退（重要）

所有改动都已进入 git，**任何一步都能退**。

```bash
# 查看回退点
git log --oneline
git tag                              # baseline-2026-09-10 是优化前的基线

# 退回某个提交（只改工作区，保留历史）
git revert <commit>

# 硬回退到基线（丢弃之后所有改动，慎用）
git reset --hard baseline-2026-09-10

# 只丢弃未提交的改动
git checkout -- <file>
```

**离库备份**（完整 git 历史，单文件，可克隆恢复）：

```
C:/Users/zanboring/OneDrive - Ormesby Primary/Desktop/recruitment-python-backups/
  └── recruitment-python-baseline-2026-09-10.bundle   (301 KB)

# 从备份恢复成一个新目录：
git clone recruitment-python-baseline-2026-09-10.bundle recruitment-python-restored
```

备份目录在 OneDrive 同步范围内，已有异地副本。**建议每次大改动后重新生成一份 bundle。**

---

## 4. 架构导览

```
app/
├── main.py                 应用入口；**中间件顺序在这里，改动前先读注释**
├── config.py               全部配置项（都有注释说明为什么需要）
├── routers/                HTTP 层（鉴权边界、参数校验）
│   ├── ai.py               对话 / SSE 流式 / 取消
│   ├── model.py            模型状态、切换、**用量成本统计**
│   ├── crawler.py          爬取任务、**支持范围查询**
│   ├── compat.py           ★ Java 版前端兼容层（21 个别名接口）
│   └── ...
├── services/
│   ├── ai_service.py       ★ AI 编排：降级链、工具调用、用量记录
│   ├── llm_client.py       ★ 云端统一调用层（OpenAI 兼容 + 多供应商容灾）
│   ├── ollama_client.py    ★ 本地统一调用层（按角色路由两个本地模型）
│   ├── usage_service.py    ★ Token 用量与成本统计
│   ├── tool_service.py     Function Calling + **工具识别前置过滤**
│   ├── knowledge_service.py 语义检索 + 关键词降级 + RRF 混合
│   ├── embedding_service.py 向量化（云端 / 本地可切换）
│   └── ...
├── middleware/             限流、请求上下文(contextvars)、camelCase 字段兼容
├── evaluation/             ★ RAG 评估（黄金集 + 指标 + 多策略对比）
├── crawlers/               BOSS 爬虫 + **城市白名单** + 清洗器
├── recommender/            Jaccard 匹配、薪资预测、多因子分析
└── utils/                  日志装饰器、job_key 指纹、安全与时间工具

scripts/
├── init_db.py              建表 + 初始化管理员
├── eval_rag.py             RAG 检索效果评估（输出 Markdown 报告）
└── bench_local_models.py   本地模型能力对比（决定模型分工）

reports/                    评估报告产物（可作为面试材料）
tests/                      355 项测试；25 个文件按主题划分
```

★ = 接手时优先读的文件。

---

## 5. 关键设计决策（**为什么是这样**）

这些是踩过坑之后的选择，不是随手写的。改动前请先理解原因。

### 5.1 中间件顺序（`app/main.py`）

`add_middleware` **越靠后越外层**。当前从内到外：限流 → CORS → 请求上下文 → camelCase。
- CORS 在限流外层 → 浏览器 OPTIONS 预检不占限流配额；
- 请求上下文在最外层 → 连被限流拒绝的请求也记进日志；
- camelCase 在最外层 → 覆盖所有 JSON 响应。

### 5.2 静态路由必须排在动态路径之前

FastAPI 按注册顺序匹配。`DELETE /api/jobs/batch` 排在 `/{job_id}` 之后时，
"batch" 会被当作 int 解析 → **422，接口完全不可用**。
`compat.py` 也因此必须注册在原生路由之前。回归测试：`tests/test_route_order.py`。

### 5.3 两条对话入口共用模型偏好

`/api/ai/chat-stream`（用户对话）与 `/api/model/chat`（管理端测试）都走
`model_service.local_candidates()`。**不要**让其中一条自己排序模型 ——
否则管理端切换的偏好在另一条入口上会静默失效。

### 5.4 降级链与「不要重复输出」

顺序：**云端（DeepSeek → 智谱备选）→ 本地模型（语言类→代码类）→ 规则引擎**。

流式场景一旦某个后端**已经产出过内容**才失败，就**不再降级** ——
否则用户会先看到半截回答、又看到一份完整回答。

### 5.5 本地模型按角色分工

| 角色 | 模型 | 配置项 | 实测依据 |
|---|---|---|---|
| 对话生成 | `qwen2.5:14b`（语言类） | `OLLAMA_MODEL` | 知识问答 3/3 |
| 工具识别/JSON | `qwen2.5-coder:7b`（代码类） | `OLLAMA_CODE_MODEL` | 准确率同为 100%，但更快（5.4s vs 7.4s）|

依据来自 `scripts/bench_local_models.py` 的实测，报告在 `reports/local_model_benchmark.md`。
**不要**凭感觉改成「统一用大的模型」。

### 5.6 「服务可达」≠「模型可用」

健康探测必须核对配置的模型**是否真的 pull 过**。曾出现配置写 `qwen2:7b` 而本机装的是
`qwen2.5:14b`，旧逻辑只看 `/api/tags` 返回 200 就报「已就绪」，直到调用才 404 并被静默降级。

### 5.7 统计口径必须统一为「只算在架岗位」

`job_service.VISIBLE_STATUS`。6 个图表接口与 AI 技能分析共用它；
唯一例外是「岗位状态分布」图表（它的职责就是展示各状态）。
不统一时会出现「图表里有用户看不到的城市」和「AI 与图表数字对不上」。

### 5.8 用量记录的三条原则（`usage_service.py`）

1. **写入失败绝不影响业务** —— 异常一律吞掉只记日志；
2. **费用在写入时算好并落库** —— 单价会变，历史应按当时价计价；
3. **零 token 的降级事件也要记录** —— 只统计 token 的话「降级有没有发生」永远查不出来。

### 5.9 密码哈希：`sha256$` 前缀 + SHA-256 预哈希 + bcrypt

bcrypt 只取前 72 字节。原实现 `pwd.encode()[:72]` 静默截断，实测「100 个 A」与
「72 个 A」密码完全等价，而 schema 允许 128 字符。
校验时识别前缀：无前缀的历史哈希走老路径，**老账号不需要迁移**。

### 5.10 前置校验优先于「静默给错数据」

爬取前校验平台与城市（`crawler_service.run_crawl`）。两条历史问题：
- 未收录城市曾回退北京编码 → 搜「南昌」实际爬北京岗位；
- 未实现平台被静默跳过 → 任务标成「已完成 / 0 条」，前端不知原因。

原则：**宁可让任务明确失败并写明原因，也不要返回看起来正常但内容错误的数据。**

---

## 6. 已完成的工作（按轮次）

| 轮次 | 主要内容 |
|---|---|
| 首轮审计 | 修复 3 个 P0 路由顺序缺陷；日志模块从空壳变可用（接入 19 处写操作）；补齐 10 个敏感接口鉴权；修复限流 XFF 绕过；统一爬虫双实现 / job_key / LIKE 转义 / 经验年限往返；Excel 导入幂等 |
| 前端兼容 | 新增 `compat.py`（21 个别名接口）+ camelCase 字段中间件，前后端接口差异 **30 处 → 0** |
| AI 工程化 | RAG 评估体系（黄金集 + Hit@K/MRR + 三策略对比 + 报告）；DeepSeek 接入 + OpenAI 兼容统一调用层；本地双模型角色分工 |
| 成本与延迟 | 工具识别前置过滤（省 ~2.9s/次）；Token 用量与成本统计（`ai_usage` 表 + `/api/model/usage`）；输出长度分级上限 |
| 正确性与性能 | 补索引（日志清理与 AI 检索此前全表扫描）；修复密码静默截断；修复未收录城市爬错城市、未实现平台静默跳过；后台任务持有强引用（防 GC）；统一统计口径；修复账号锁定后续重试次数 |

详细的技术原因见 `README.md` 的「已知问题与修复记录」表格。

---

## 7. 真正剩余的工作

> 已完成的项已从历史版本移除，以下均为**尚未做**的事。

### P0 · 演示与交付

1. **前端实测** —— 后端侧接口/字段/口径已对齐，但前端页面渲染未验证过。
   建议顺序：启动后端 → `cd recruitment-system-frontend && npm run dev` →
   依次点数据管理、用户管理、知识库、模型管理。
2. **默认管理员口令** —— `admin/admin123` 目前写死在 `app/init_data.py`。
   演示环境外必须改（首次启动随机密码或强制改密）。
3. **容器化** —— 无 `docker-compose.yml`。建议 API + MySQL（Ollama 可选）。

### P1 · AI 工程加分项

4. **RAG 引用溯源** —— 检索命中时让回答带上知识条目来源。
   现在上下文被注入 prompt 但用户看不到依据。改 `knowledge_service.get_context_for_ai`
   返回 `sources`，并在 SSE 里透出。
5. **语义检索的实跑数字** —— 评估体系已就绪，但**只跑出了关键词基线**：
   字面型 Hit@5 = 75%、语义型 Hit@5 = **0%**。要拿到三策略完整对比，需要
   配置智谱 Key（`EMBEDDING_BACKEND=zhipu`）或 `ollama pull nomic-embed-text`。
   **这个数字是简历上最有说服力的一条，强烈建议补上。**
6. **多工具编排** —— `TOOLS` 目前只有 `query_jobs` 一个。架构支持横向扩展，
   加「薪资对比」「公司评价」等工具能体现编排能力。

### P2 · 工程完善

7. **限流换分布式** —— 当前是单进程内存实现。多副本部署前必须换 Redis 计数器。
8. **向量持久化** —— 向量只在内存缓存，进程重启后整库重新向量化（云端要花钱）。
9. **前端改为动态拉取可选项** —— 前端硬编码 4 个平台 / 11 个城市，
   而后端只实现 1 个平台 / 收录 20 个城市。后端已提供 `GET /api/crawler/options`，
   前端应据此渲染。

---

## 8. 踩过的坑（供接手时避雷）

- **批量脚本改文件必须读回校验**：曾出现 7 处替换全部报 OK、但落盘后常量与引用全丢。
  现在改成「替换 → 写盘 → 读回统计关键字出现次数」。
- **`grep -A30 <函数>` 会串到下一个函数**：核对某函数自身内容时应先按函数切分再 grep，
  否则会得出「它过滤了 status」这类错误结论（我确实栽过一次）。
- **前端有 4 份副本**：`Recruitment` / `Recruitment outside/Recruitment midle/Recruitment` 等。
  用修改时间戳与后端改动时间对齐来判断哪份是活跃副本，改错目录等于白干。
- **写「接口对比」类脚本要过滤注释**：否则会扫出已注释掉的代码造成假阳性。
- **测试桩要能吞掉新增的关键字参数**：给函数加参数时，旧桩用固定签名会失败。
  本项目约定测试桩写成 `def fake(..., **kwargs)`。
- **改哈希方案后要同步老断言**：`test_auth_api.py` 曾断言 `startswith("$2")`。

---

## 9. 面试讲解提纲

> 这段可以直接背，但**务必配合代码讲**，否则听起来像套话。

**开场（30 秒）**
「这不是一个把 LLM 当聊天框接进来的项目。我做的是一个**可评估、可观测、可控成本**的
AI 应用：招聘数据来自真实爬取，AI 能调用工具查真库、能做 RAG，而且我知道它的
检索效果是多少、每次调用花多少钱、降级有没有真的生效。」

**技术亮点（挑 3 个展开）**

1. **用数据说话，而不是「感觉向量检索更先进」**
   「我建了黄金测试集把查询分成字面型和语义型两类，实测关键词检索在语义型查询上
   命中率是 **0%** —— 这就是我引入向量检索的量化依据。混合检索我用 RRF 按排名融合，
   因为余弦相似度和关键词匹配的分数量纲不可比，直接加权要调参且不稳。」

2. **成本与可观测性**
   「每次 LLM 和向量化调用都记录 token、耗时、降级层级与失败原因，落库后按场景/模型/
   降级层级聚合。我能回答『贵在哪』『降级生效了吗』。另外我发现工具识别对每条消息都
   发起一次额外 LLM 调用，实测 2.9 秒，而大量消息根本不涉及数据库 —— 加了一层廉价
   预过滤，规则刻意保守，因为漏判的代价是模型凭空编数字。」

3. **本地模型的角色分工**
   「我本地有两个模型，但我没有简单地把它们当成互为备份。我先写了基准脚本实测它们在
   工具识别和知识问答上的差异，发现小模型在结构化抽取上准确率相同但快一倍，大模型在
   事实准确性上更好 —— 于是按任务分工路由。同时我发现原来的健康检查只探测服务可达、
   不校验模型是否存在，导致配置错误被静默降级掩盖。」

**主动暴露的取舍（显得可信）**
「限流目前是单进程内存实现，多副本部署前必须换 Redis；向量只在内存缓存，重启后要
重新向量化；语义检索的完整评估数字还差一个 embedding Key —— 这些我都写在 README 的
待办里了。」

**不要说的话**
- 不要说「实现了 RAG」就结束 —— 要给出检索效果的**数字**。
- 不要说「用了降级链」就结束 —— 要说**降级有没有真的发生过，你怎么知道的**。
- 不要宣称与 Java 版「完全等价」—— Python 版以 AI 工程能力取胜。

---

## 10. 重要安全约束（不要改回去）

- `APP_ENV=production` 时启动会拒绝默认 / 过短的 `JWT_SECRET`。
- 系统环境变量优先于 `.env`（部署平台注入的密钥不会被仓库内 `.env` 覆盖）。
- AI 会话键是 `user_id + session_id`，**不能**改回只用 `session_id`（会跨用户串话）。
- `X-Forwarded-For` 默认**不采信**（客户端可伪造，用作限流维度等于换头换配额）。
  确实在可信代理后时用 `RATE_LIMIT_TRUST_FORWARDED_FOR=true` 显式打开。
- 自动入库记录**真实来源**（模型名），便于与人工内容区分；
  答错内容会被固化进知识库并反复召回，需要时用 `AI_AUTO_LEARN_ENABLED=false` 关掉。
- 不要提交 `.env`、真实 API Key 或数据库文件（`.gitignore` 已覆盖）。
