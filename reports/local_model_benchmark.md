# 本地模型能力对比（实测）

- 服务地址：`http://localhost:11434`
- 工具识别使用项目真实提示词与解析器（`app/services/tool_service.py`）
- 知识问答的通过判定为关键词启发式，仅供粗略参考，结论以「模型原话」为准

## 一、汇总对比

| 模型 | 工具识别准确率 | 工具识别平均耗时 | 知识问答通过 | 生成速度 |
|---|---|---|---|---|
| `qwen2.5-coder:7b` | 100% | 5.4s | 2/3 | 72.2 tok/s |
| `qwen2.5:14b` | 100% | 7.4s | 3/3 | 12.2 tok/s |

## 二、`qwen2.5-coder:7b` 明细

### 工具识别

| 提问 | 期望 | 实际 | 判定 | 耗时 |
|---|---|---|---|---|
| 长沙有多少Java岗位 | call | call | ✅ | 14.9s |
| 深圳前端岗位薪资水平怎么样 | call | call | ✅ | 3.6s |
| 帮我查查北京的Python岗位 | call | call | ✅ | 3.6s |
| 你好，你是谁 | none | none | ✅ | 3.4s |
| 介绍一下这个系统 | none | none | ✅ | 3.5s |
| RAG是什么 | none | none | ✅ | 3.3s |

### 知识问答

| 提问 | 回答 | 判定 |
|---|---|---|
| 一句话说明什么是 RAG | RAG 是一种结合了 Retrieval 和 Generation 两种技术的模型，用于从大量文本数据中检索相关信息并生成答案。 | ❌ 未命中期望关键词 |
| 一句话说明 Jaccard 相似度怎么算 | Jaccard 相似度是通过计算两个集合的交集与并集的比值来衡量它们之间的相似程度。 | ✅ |
| 一句话说明 SSE 和 WebSocket 的主要区别 | SSE（Server-Sent Events）是服务器向客户端推送实时更新的一种技术，而 WebSocket 是全双工通信协议，支持客户端和服务器之间的双向通信。 | ✅ |

## 二、`qwen2.5:14b` 明细

### 工具识别

| 提问 | 期望 | 实际 | 判定 | 耗时 |
|---|---|---|---|---|
| 长沙有多少Java岗位 | call | call | ✅ | 25.7s |
| 深圳前端岗位薪资水平怎么样 | call | call | ✅ | 4.9s |
| 帮我查查北京的Python岗位 | call | call | ✅ | 4.9s |
| 你好，你是谁 | none | none | ✅ | 2.9s |
| 介绍一下这个系统 | none | none | ✅ | 2.9s |
| RAG是什么 | none | none | ✅ | 3.0s |

### 知识问答

| 提问 | 回答 | 判定 |
|---|---|---|
| 一句话说明什么是 RAG | RAG（检索增强生成）是一种结合了信息检索和语言模型生成的技术，它允许模型在回答问题或生成文本时，先从外部文档或互联网中检索相关信息，再基于这些信息进行生成，从而提高回答的准确性和相关性。 | ✅ |
| 一句话说明 Jaccard 相似度怎么算 | Jaccard相似度通过计算两个集合的交集大小除以它们的并集大小来衡量集合之间的相似度。公式为：J(A, B) = \|A∩B\| / \|A∪B\|。 | ✅ |
| 一句话说明 SSE 和 WebSocket 的主要区别 | SSE（Server-Sent Events）是单向的，服务器向客户端推送事件，而WebSocket支持双向通信，允许客户端和服务器互相发送数据。 | ✅ |

## 三、结论与用法

结合上表的实测差异，本地两个模型按下述方式分工（配置见 `app/config.py`）：

- **对话生成**用语言类模型（`OLLAMA_MODEL`）——语言表达更自然，事实准确率更高
- **工具识别 / 结构化抽取**用代码类模型（`OLLAMA_CODE_MODEL`）——
  准确率与语言模型相同但耗时约减半，且严格 JSON 输出更稳定
- 两者都未安装时，本地这一级整体不可用，降级链会直接跳到规则引擎
