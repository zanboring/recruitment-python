# 面试答辩材料

> 这份文档的每一条都能在代码里找到出处。**不要背结论，要能当场打开文件指出那一行。**
> 想不公开就把它加进 `.gitignore` 或推 GitHub 前删掉。
>
> 标记说明：`✅ 实测` = 我实际跑过；`📄 代码` = 可在指定文件里看到；`📝 文档` = 项目文档里记录的。

---

## 一、开场（30 秒，先建立「这不是 CRUD」的印象）

> 「这是一个招聘数据 + AI 分析系统。它不是把 LLM 当聊天框接进来 ——
> 我做的是**可采集、可评估、可观测、可交付**的完整链路：
> 数据从 4 个招聘平台真实采集进来，AI 能调用工具查真库、能做 RAG 检索，
> 而且我知道它的检索效果是多少、每次调用花多少钱、降级有没有真的生效。
> 最后它有 664 项测试、能打成零依赖的绿色软件、也有版本与更新机制。」

**为什么这样开场**：一句话把「采集 → AI → 工程化」三层都点到，面试官会自己挑一层追问。
你只要保证三层都能接住。

---

## 二、数字弹药（可信度锚点，背下来）

| 数字 | 含义 | 怎么验证 |
|---|---|---|
| **664** | 全量测试数，全绿 | `python -m pytest -q` ✅ 实测 |
| **98** | API 端点（按 methods 计） | `python -c` 遍历 `app.routes` ✅ 实测 |
| **4 / 2** | 4 个平台有采集能力，其中 2 个有服务端爬虫 | `/api/crawler/options` 📄 |
| **8** | 日报 Excel 的 sheet 数（含数据透视） | `report_service.build_excel` 📄 |
| **0% → 43.8% → 100%** | 关键词检索在**语义型**查询上的 Hit@5 是 0%；
语义向量提到 43.8%；RRF 混合在**字面型**上做到 100% | `scripts/eval_rag.py` 📄📝 |
| **9** | 我在这个项目里发现并修掉的真实缺陷数 | 见第五节 📄 |

> **用法**：不要一口气全报。被问到相关话题时抛一个，然后等追问。
> 「0%」这个数字最有冲击力 —— 先记它。

---

## 三、六个技术点（每个都按「问题 → 选择 → 代价」讲）

### 1. 用数据决定要不要上向量检索，而不是"因为先进"

- **问题**：项目一开始是关键词检索。要不要花成本上 RAG？
- **做法**：先建黄金测试集，把查询分成**字面型**和**语义型**两类，跑离线基线。
  实测关键词检索对语义型查询 **Hit@5 = 0%** —— 用户问「工资」，条目里写的是「薪资」，
  字面完全对不上。这才是上向量检索的量化依据，而不是"感觉更先进"。
- **代价**：向量化有成本（云端按量计费）。所以加了**结果缓存 600 秒 + 单条向量缓存 1 小时**，
  并且提供了 `EMBEDDING_BACKEND=ollama` 走本地模型（零成本、数据不出内网）。
- 📄 `app/evaluation/`、`app/services/embedding_service.py`、`README` 的 RAG 章节

### 2. 混合检索用 RRF 而不是加权求和

- **问题**：余弦相似度和关键词匹配的分数量纲完全不同，怎么融合？
- **做法**：用 **RRF（Reciprocal Rank Fusion）** —— 只按各自**排名**融合
  （`score = Σ 1/(60 + rank)`），彻底绕开量纲问题。
- **代价**：放弃了"调权重能更精细"的可能。但直接加权需要人工调参且不稳定，
  换一批数据就得重调；RRF 无需调参、跨数据集稳定。**这是拿精细度换稳定性。**
- 📄 `app/services/knowledge_service.py`

### 3. 三级降级链 + 熔断器

- **问题**：云端 API 会失败（超时、额度用尽、被限流）。失败时不能整个功能挂掉。
- **做法**：云端 → 本地 Ollama → 规则引擎，三级降级，且**如实标注实际走到了哪一层**
  （响应里带 `generated_by`）。另加熔断器：连续失败就暂时不再打云端，避免长时间卡在超时上。
- **代价**：规则引擎的回答质量明显差。但**"降级了但没告诉你"比"降级"危害大得多** ——
  所以宁可回答朴素，也要把层级透出来。
- 📄 `app/services/llm_client.py`、`ai_service.py`、`local_model_service.py`

### 4. 反爬：两层护栏，而且知道哪层解决哪个问题

- **第一层（组内）**：随机延时、指数退避+抖动、完整浏览器请求头（UA 与 `Sec-CH-UA`
  平台协变 —— 只换 UA 会被识别为自动化）、风控信号检测后自适应放大延迟。
- **第二层（跨任务）**：这是第一层**覆盖不到的盲区** ——
  - **同一域名串行** + 最小间隔：只加延迟挡不住两个任务"各自等够时间后同时发请求"，
    对站点而言并发依然是 2，必须串行；
  - **单平台日配额**：回答"我今天最多访问多少次"；
  - **robots.txt 门禁**（按域名缓存 1 小时）；
  - **尊重 `Retry-After`**：秒数和 HTTP 日期两种格式都解析 ——
    只处理前者是常见疏漏，对方说了等多久却不听，很容易把"临时限流"升级成"封禁"。
- **代价**：默认对 robots 只告警不阻断（`CRAWL_ROBOTS_STRICT=false`）。
  这是刻意取舍：默认阻断会让人撞上"配置全对却跑不起来"且分不清是自己配错还是站点不允许。
  **无论哪种模式，命中 Disallow 都会记 WARNING 并写进任务 message，绝不静默通过。**
- 📄 `app/crawlers/base.py`、`app/crawlers/throttle.py`、`app/crawlers/robots.py`

### 5. 「用前端代码爬取」——纯页面 JS 不行，油猴行

这一条最能体现你**真的理解浏览器**，而且很多人答不上来。

- **纯页面 JS（控制台/书签）不可行**，原因不是"难"而是**不可能**：
  同源策略 + CORS 拦掉跨域 `fetch`；`mode:'no-cors'` 只能拿到 **opaque 响应读不到内容**；
  带自定义头会触发**预检**并被拒；JSONP 现代站点早已不提供。
- **可行的是油猴脚本/浏览器扩展**：`GM_xmlhttpRequest` 是**特权 API，不受同源策略限制**。
- **顺带解决抗封**：请求发自用户真实浏览器（真实登录态、真实 IP、真实指纹），
  **"集中 IP 访问"这个特征根本不存在**，也就没有被封的触发条件。
  一句话：**服务端爬虫是在跟反爬对抗；油猴脚本是绕开了对抗的前提。**
- **一个顺手得到的安全设计**：回传用**自定义头**携带令牌。自定义头会触发 CORS 预检，
  而普通网页拿不到预检许可、**根本发不出这个请求**；油猴不受此限。
  等于借浏览器自己的同源策略，挡掉了"任意网站偷偷往 `localhost:8080` 灌数据"。
- 📄 `userscript/recsys-collector.user.js`、`userscript/README.md`

### 6. 能被面试官追问的工程细节（挑你熟的准备）

| 点 | 一句话 |
|---|---|
| 版本比较**必须按数字** | 字典序下 `"1.10.0" < "1.9.0"`，会把新版判成旧版。有专门用例 |
| 检查更新**永不抛出** | 网络挂了/被限流/还没发过 Release，都以状态字段如实返回，不打 500 |
| 时区 | 存储统一 naive UTC，"今天"按北京时区算 —— 否则 00:00–08:00 的日报会记到昨天 |
| 路由顺序 | 静态路径必须排在 `/{id}` 之前，否则 `batch`/`stats` 会被当 int 解析成 422 |
| 下架判定有宽限期 | 换关键词爬取时不能把"刚被别的关键词刷新过"的岗位误判下架 |
| 测试隔离 | 两个 AI 助手共用一个工作区时，`reset --hard` 会静默冲掉别人未提交的改动 —— 已用 `git worktree` 拆开 |

---

## 四、【最重要】「这些是你自己写的吗 / 是不是 AI 写的」怎么答

**不要否认用了 AI** —— 那是 2026 年，否认反而不可信。要**转移战场**：

> 「我大量用 AI 写代码，这一点我不避讳。但我想说的是另一件事：
> **AI 的输出需要有人能验证对不对，而这个项目里我发现了 9 个真实缺陷。**
> 其中最有代表性的一个 —— 项目原本有 466 项测试全绿，但爬虫**抓到的数据一条都没入库**。
> 因为测试把关键函数整个替换成了桩函数，**真实代码一次都没被执行过**。
> 测试全绿和数据全丢可以同时成立，这就是我说"要能验证"的意思。」

**为什么这个回答有力**：它证明的**不是你会写代码，而是你知道测试什么时候在骗你**。
这是初级和高级之间最真实的分界线。

### 九个缺陷（按「能讲出什么」排序，你至少要熟前三个）

| # | 缺陷 | 能证明什么 |
|---|---|---|
| 1 | **爬虫 `crawl()` 少一个 `return`** → 抓到的岗位全部被丢弃，任务状态却是"已完成"。**466 项测试全绿** | **测试有效性**：桩函数替代了真实路径，测试就没在测东西 |
| 2 | **绿色版 exe 在干净机器上起不来**：建表只写在 `scripts/init_db.py`，打包后的 exe 没有任何建表入口，发布包也不带预置数据库 → 空库启动 `no such table: user` | **交付思维**：核心卖点是"零依赖即用"，但从没在干净环境验证过 |
| 3 | **测试不 hermetic**：`conftest` 只清了一个云厂商的 key，本机配了另一个 key 时，降级类用例会失败 —— **结果随开发者本机配置漂移** | **环境隔离**：测试不该依赖开发者的 `.env` |
| 4 | **SQLite 下 import 就崩**：无条件给 `create_async_engine` 传 `pool_size`，而 SQLite 内存库走 StaticPool 不接受该参数。**而 SQLite 正是绿色版的默认库** | **方言差异** |
| 5–7 | **配置写入三缺陷**：保存后把 `config.env` 的**注释全删了**（而文档正是引导用户复制带注释的模板）；键名大小写不归一导致**同一配置写两行**；值未转义换行**可注入任意配置行** | **边界与转义**：用户输入进文件时的转义是基本纪律 |
| 8 | **上面的修复引入了新问题**：抛出的 `ValueError` 没被路由捕获，用户粘贴带换行的 key 会看到 **HTTP 500「服务器内部错误」**，而不是那句可操作的提示 | **修复也要 review**：修好一个静默问题，可能在出口处换个不可诊断的 500 |
| 9 | 协作文档被**整份复制成两份**（解冲突时把两个版本拼接而非合并） | **过程纪律** |

> 注意 5–8 的诚实版本：**这 4 个是我 review 出来的，由协作者修的。**
> 说实话反而更好 —— 说明你会读别人的代码。硬说成自己修的，
> 被追问"你改的哪一行"就会露馅。

### 追问预案

- **「AI 写的代码你怎么保证质量？」**
  → 举 #1：**测试全绿不等于测到了东西**。我的做法是先问"这些测试真的在测那条路径吗"，
  再补一条真实执行 `crawl()` 的用例。而且我会**验证回归测试本身有效** ——
  把修复撤掉，确认测试必须失败（不然就是个永远绿的假回归）。
  项目里 `test_startup_bootstrap.py` 我就是这么验过的。✅ 实测

- **「你觉得 AI 编程的边界在哪？」**
  → 「AI 很擅长写**有明确规格**的代码，比如实现一个已知算法的适配器。
  它不擅长的是**判断这里该不该这么做** —— 比如"这个测试是不是在骗我"、
  "绿色版到底能不能在干净机器上跑起来"。这两个问题它都没主动发现，是我发现的。」

- **「你最大的短板是什么？」**（别答"太追求完美"这种）
  → 「我写代码的经验确实少，所以我现在刻意在练**两件事**：
  一是写完先想"这个改动会在哪里失败"，二是把每个修复都补一条回归测试。
  这个项目里我修的每个缺陷都配了测试，其中两个我特意验证过"撤掉修复必须失败"。」

---

## 五、主动暴露的不足（说在前面，显得可信）

1. **采集的选择器没在真实站点验证过** —— 开发环境无法访问外网，脚本里的选择器是按
   站点公开结构写的有依据的猜测。所以脚本提供了**预览模式**（只识别不发送，
   并告诉你用的是哪个选择器、还是走了兜底定位）。这条我如实写在了 `userscript/README.md`。
2. **限流与配额是进程内实现**，多副本部署需要换 Redis 计数器。
   （取舍：单实例部署够用，换 Redis 的复杂度不值得现在付。）
3. **`app/routers/jobs.py` 里 `validate` 字段遮蔽了 Pydantic 的 `BaseModel.validate`**，
   启动会打两条 UserWarning，Pydantic v3 会变成硬错误。
   **我没直接改名，因为它是对外契约**（前端传 `validate=true`），
   正确做法是 `Field(alias="validate")` + `populate_by_name=True`。📄 已在 README 记录

> **为什么主动说不足**：面试官一定会挖。你先说了，就成了"有工程判断"；
> 被挖出来，就成了"藏着问题"。

---

## 六、快速自查清单（面试前一晚过一遍）

- [ ] 能打开 `app/crawlers/base.py` 指出「组内」护栏在哪、`throttle.py` 指出「跨任务」护栏在哪
- [ ] 能用自己的话说清「为什么只加延迟不能防并发」
- [ ] 能说清 CORS 为什么让纯页面 JS 采集不可行、油猴为什么可行
- [ ] 能讲 #1 缺陷（测试全绿 + 数据全丢），包括"桩函数替代真实路径"这个原因
- [ ] 能解释 RRF 为什么不需要调参（量纲不可比 → 只按排名融合）
- [ ] 能说出 664 这个数字，以及它**不能**证明什么（不能证明测到了真实路径）
- [ ] 准备好"我用 AI 但不只会用 AI"的那段话（第四节开头，一字不差地练三遍）

---

## 附：常用命令（面试时可能要现场演示）

```bash
python -m pytest -q                    # 全量测试（预期 664 项全绿）
python scripts/init_db.py              # 建表 + 默认管理员
python -m uvicorn app.main:app --port 8080    # 启动服务
python scripts/eval_rag.py --strategies keyword   # 离线跑检索基线（无需密钥）
build-exe.bat                          # 打包绿色版 → dist/RecSys/
```

**演示前务必先跑一次 `python -m pytest -q`** —— 现场红一个测试比不演示更糟。
